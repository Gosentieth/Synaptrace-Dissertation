# Player Animation

The current animation foundation is runtime procedural and is implemented by `PlayerVisualAnimator`.

Future authored player animation clips and Animator Controllers should be stored in this folder after the runtime-created player visual is migrated to a prefab.
